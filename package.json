{
    "devDependencies": {
        "typescript": "^3.7.5"
    },
    "dependencies": {
        "axios": "^0.21.1"
    },
    "scripts": {
        "start:db": "json-server -w db.json -p 4000",
        "start:parcel": "parcel index.html"
    }
}